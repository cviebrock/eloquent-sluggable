# Travis CI configuration

language: php

services:
  - redis-server

php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - travis_retry composer self-update
  - travis_retry composer install --dev

script: phpunit
